# T01 — reproducible foundation

Date: 2026-09-05. Branch: `codex/implementation`; base: `7ecb88c`.

## Delivered

pnpm workspace, pinned runtime/dependencies and lockfile, strict TypeScript, ESLint,
Fastify health with no DB/provider dependency, development/production entry points,
React/Vite startup shell with real health status, retry and RU/EN switching,
responsive initial surface, Vitest/Playwright harness and changed-code CI definition.
No production fake world or fake provider is included.

## Observed checks

- `pnpm install --reporter append-only`: success, 211 packages, no peer conflicts.
- `pnpm exec node --version`: v24.20.0 (system Node remains v24.13.0).
- Before implementation, `pnpm exec vitest run tests/tasks/T01.test.ts` failed because the server module did not exist. This was a module-loading failure, not an assertion failure.
- After implementation, same targeted command: 2 tests passed, exit 0.
- `pnpm build`: API TypeScript and Vite production build succeeded.
- `pnpm typecheck`, `pnpm lint`: exit 0.
- First browser attempt: 2 infrastructure failures, required Chromium 1243 not installed; application assertions did not execute.
- `pnpm exec playwright install chromium`: installed the pinned Chromium.
- `pnpm exec playwright test tests/e2e/T01-startup.spec.ts`: 2 passed in 6.2s; real API, RU/EN, failed connection and retry, 390px reflow.
- Viewed desktop 1280×720 and mobile 390×844 screenshots. Text/controls remain inside the window and viewport, no overlap/clipping observed. This is startup-shell review only, not T33 game UI approval.
- `git diff --check`: exit 0.

Screenshots: `test-results/T01-startup-startup-report-d47da-ts-both-interface-languages-chromium/startup-desktop.png` and `startup-mobile.png` (local ignored artifacts, regenerated by the focused browser test).
Browser run emitted inherited NO_COLOR/FORCE_COLOR warnings; they are not application errors.

## Dependency decisions and primary references

Registry metadata was checked for exact versions and peer dependencies. TypeScript 7.0.2 was not selected because typescript-eslint 8.69.0 declares TypeScript <6.1.0; selected 6.0.3.
Node 24.20.0 came from the official Node release index (2026-08-26, Krypton LTS).

- https://nodejs.org/dist/index.json
- https://pnpm.io/10.x/settings#usenodeversion
- https://vite.dev/guide/
- https://vitest.dev/guide/
- https://fastify.dev/docs/latest/Guides/Testing/

## Review and remaining work

Reviewed inline against T01; no subagents dispatched because the accepted execution plan requires sequential work without separately authorized agents.
The startup shell deliberately labels itself a development environment. It is not the complete XP window manager, game or release.
Content/evaluation/release script names are reserved in package.json; their tools belong to T05/T32/T36 and are explicitly unavailable until implemented.
CI definition has not been run on GitHub. A clean local checkout validation is the next T01 check.
Full-suite runs consumed: 0 / 3. These checks target the new foundation only.
