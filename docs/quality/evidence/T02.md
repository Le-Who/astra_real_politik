# T02 — domain foundation (IN PROGRESS)

Date: 2026-09-06. Branch: `codex/implementation`; previous foundation commit: `cfa4513`.

## Implemented subset

- Zod schemas: IDs, calendar dates, visibility, evidence/facts, actors, territorial control, directed relations, resource transfers, conflicts, world snapshots, operation/event/proposal envelopes, commands, rooms/messages/treaties and model profiles.
- Strict objects and bounded strings/numbers/arrays/entity dictionaries; versioned proposal/snapshot envelopes; custom model IDs remain open, not restricted to presets.
- `toProviderSchema`: native Zod JSON Schema conversion, explicit keyword allowlist, rejects unsupported structures. Actual Gemini capability matching remains T10 work; local semantic refinements cannot be delegated to JSON Schema.
- `validateProposal`: references, chronology, local causal order, control conflicts, typed factual predicates, evidence registry and private-fact visibility; normalized domain operations require trusted server validation.
- `canonicalize`: deterministic IDs, local-to-canonical causal references, journaled clock progression even with no narrative events.
- `reduce`/`replay`: immutable world projection, contiguous revision groups, campaign/job identity, duplicate rejection, causal-order rejection. No AI dependencies or calls.

## Observed verification

- Initial targeted T02 run: failed loading the not-yet-created contracts module (not an assertion red).
- Initial contracts compilation exposed missing platform types for URL/AbortSignal. Explicit browser-standard library types added to the shared package; engine explicitly uses Node types.
- First executable suite: 16 passed, 2 failed. A prototype-related input key was sanitized by Zod before the dictionary refinement; added bounded input-key inspection before parsing. The second failure was an overly exact issue-array assertion: backward intervals legitimately produce more than one temporal issue; assertion now checks presence of the required error without limiting other valid errors.
- Inline review identified replay accepting self-causal canonical events. New focused regression test failed with "expected function to throw" before the fix. Reducer now checks supplied causal IDs against already-seen events, rejecting self/forward links while permitting references to history preceding a supplied snapshot.
- Final `pnpm exec vitest run tests/tasks/T02.test.ts`: **20 passed**, exit 0. Includes 40 deterministic fast-check generated relation updates (seed 20260905).
- `pnpm --filter @astra/contracts build` and `pnpm --filter @astra/engine build`: succeeded; latest engine build includes the causal fix.
- `pnpm exec tsc --noEmit`: exit 0.
- `pnpm exec eslint packages tests/tasks/T02.test.ts tests/fixtures/world.ts eslint.config.js vitest.config.ts --max-warnings 0`: exit 0.
- `git diff --check`: exit 0.
- Vitest resolves workspace contracts directly to source to avoid accidentally testing stale compiled code. Production packages emit separate JS/declarations.

## Deliberately incomplete — do not mark T02 or the game complete

- `scenario.ts` package manifest schema and complete AI request/result runtime validation still required.
- Resource/conflict/treaty/action projection coordination with normalized DB records remains T03/T18/T19 work. Core reducer throws `EXTERNAL_PROJECTION_REQUIRED`, never silently drops those effects.
- `validateExternalOperation` is a trusted integration boundary, not implemented consent/ledger/authority logic.
- Broader property tests, import integrity validation, large content coverage and all real-provider behavior remain outstanding.
- Replay causal references preceding the provided snapshot require caller-side verification against durable history; full archive validation is not implemented here.
- No real scenarios, PostgreSQL transactions, game UI, real AI calls or release gates have been claimed as passing.

Reviewed inline because the project plan requires sequential execution without separate agents. Primary schema documentation: https://zod.dev/api and https://zod.dev/json-schema. Package versions were checked against registry metadata.

Full-suite runs consumed: **0 / 3**. All runs here were scoped to T02 or changed packages; skipped tests in the single-test diagnostic run were not claimed as passed.
